# --------------------------------------------------
# Sentinel-CLI :: Environment Configuration
# --------------------------------------------------
# Copy this file to `.env` and fill in real values.
# NEVER commit the actual `.env` file.
# --------------------------------------------------

# ---- General ----
ENV=development
LOG_LEVEL=info

# ---- Execution Plane ----
# Default log source (overridden by CLI args)
LOG_SOURCE=system_mock.log

# ---- Agent Configuration ----
AGENT_WINDOW_SECONDS=60
AGENT_BRUTEFORCE_THRESHOLD=5

# ---- Policy Engine ----
POLICY_FILE=agents/policies.yaml

# ---- Future: LLM / Agentic Reasoning ----
# Leave empty unless explicitly enabled
LLM_ENABLED=false
LLM_PROVIDER=ollama
LLM_MODEL=llama3
LLM_ENDPOINT=http://localhost:11434

# ---- Security Guardrails ----
# Explicit allowlist for actions the agent may invoke
ALLOWED_ACTIONS=observe,isolate_source,collect_forensics
